# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DailyUseWord' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DailyUseWord
	#iCarousel
    pod 'iCarousel', '~> 1.8'
    pod 'SwiftOCR', '~> 1.1'
#    pod 'ZoomImageView'
end
